# API Contracts

Web·API·Worker가 공유하는 런타임 검증 스키마와 TypeScript 타입입니다.

현재 MVP 계약은 신규 영업 건 등록·조회와 첫 연락 완료 기록을 포함합니다.

```bash
pnpm --filter @tnc-crm/contracts build
pnpm --filter @tnc-crm/contracts typecheck
```
